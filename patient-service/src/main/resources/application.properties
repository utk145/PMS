spring.application.name=patient-service

# h2 in-memory database
# this provides the endpoint that we can use to view the actual content in the database
# uncomment this later if running patient-service outside of docker to use in memory data base
#spring.h2.console.path=/h2-console
#spring.h2.console.enabled=true
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driver-class-name=org.h2.Driver
#
#spring.datasource.username=admin_viewer
#spring.datasource.password=password
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Show SQL queries
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate SQL logs
logging.level.org.hibernate.SQL=DEBUG

# Hibernate parameter binding logs
logging.level.org.hibernate.orm.jdbc.bind=TRACE


# to sync the database with the entity classes. Not recommended in production!
#spring.jpa.hibernate.ddl-auto=update
# it will perform the checks against the database and our entities every time we start the application
#spring.sql.init.mode=always

logging.level.root=INFO

server.port=4000